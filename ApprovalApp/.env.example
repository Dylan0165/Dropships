# URL van de UIcontrol backend (zet dit naar je VPS als je live gaat)
VITE_API_URL=http://localhost:3001

# 4-cijferige PIN voor login op de approval app
# Zet ook APPROVAL_PIN in UIcontrol/.env zodat de backend hetzelfde gebruikt
VITE_APPROVAL_PIN=1234
