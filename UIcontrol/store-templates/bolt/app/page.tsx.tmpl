'use client';
import { useRef, useState, useEffect } from 'react';

interface Product { id: string; title: string; image: string; price: number; compareAtPrice?: number; badge?: string; description?: string }
const PRODUCTS: Product[] = {{PRODUCTS_JSON}};
const PRIMARY = '{{PRIMARY}}';
const ACCENT  = '{{ACCENT}}';

const CHECKOUT_API = '{{CHECKOUT_API_URL}}';
const STORE_ID = '{{STORE_ID}}';
const SUBDOMAIN = '{{SUBDOMAIN}}';
const RUN_ID = '{{RUN_ID}}';

async function startCheckout(p: Product, setLoading: (id: string | null) => void): Promise<void> {
  setLoading(p.id);
  try {
    const r = await fetch(CHECKOUT_API, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        storeId: STORE_ID, subdomain: SUBDOMAIN, runId: RUN_ID,
        amountEur: p.price, description: p.title,
        items: [{ id: p.id, title: p.title, price: p.price }],
      }),
    });
    if (!r.ok) throw new Error('checkout failed');
    const data = await r.json() as { checkoutUrl?: string };
    if (data.checkoutUrl) window.location.href = data.checkoutUrl;
    else throw new Error('geen checkout url');
  } catch (err) {
    console.error('[checkout]', err);
    alert('Er ging iets mis bij het afrekenen. Probeer het opnieuw.');
    setLoading(null);
  }
}

function useReveal() {
  const ref = useRef<HTMLDivElement>(null)
  const [visible, setVisible] = useState(false)
  useEffect(() => {
    const el = ref.current; if (!el) return
    const obs = new IntersectionObserver(([e]) => { if (e.isIntersecting) { setVisible(true); obs.disconnect() } }, { threshold: 0.12 })
    obs.observe(el)
    return () => obs.disconnect()
  }, [])
  return { ref, visible }
}

const reveal = (visible: boolean, i: number) => ({
  opacity: visible ? 1 : 0,
  transform: visible ? 'none' : 'translateY(20px)',
  transition: `opacity 0.6s cubic-bezier(.22,1,.36,1) ${i * 110}ms, transform 0.6s cubic-bezier(.22,1,.36,1) ${i * 110}ms`,
})

export default function Home() {
  const [heroLoaded, setHeroLoaded] = useState(false)
  const [hoveredProduct, setHoveredProduct] = useState<number | null>(null)
  const [loadingId, setLoadingId] = useState<string | null>(null)
  const productsReveal = useReveal()
  const uspReveal = useReveal()
  const reviewsReveal = useReveal()

  useEffect(() => { const t = setTimeout(() => setHeroLoaded(true), 50); return () => clearTimeout(t) }, [])

  const year = new Date().getFullYear();
  return (
    <div style={{ background: '#0a0a0a', color: '#fff', minHeight: '100dvh', fontFamily: 'inherit' }}>

      <style dangerouslySetInnerHTML={{ __html: `
        @keyframes fadeUp { from { opacity:0; transform:translateY(24px) } to { opacity:1; transform:translateY(0) } }
        .bolt-nav-link { transition: color 0.2s ease; }
        .bolt-nav-link:hover { color: #fff !important; }
        .bolt-btn { transition: transform 0.25s cubic-bezier(.22,1,.36,1), box-shadow 0.25s cubic-bezier(.22,1,.36,1); }
        .bolt-btn:hover { transform: translateY(-2px); }
        .bolt-btn:active { transform: scale(0.98); }
        .bolt-buy-btn { transition: transform 0.25s cubic-bezier(.22,1,.36,1), box-shadow 0.25s cubic-bezier(.22,1,.36,1); }
        .bolt-buy-btn:hover { transform: translateY(-2px); box-shadow: 0 4px 16px rgba(0,0,0,0.4); }
        .bolt-buy-btn:active { transform: scale(0.98); }
      ` }} />

      {/* Nav */}
      <nav style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', padding: '1.25rem 2.5rem', background: 'rgba(17,17,17,0.9)', backdropFilter: 'blur(12px)', borderBottom: `2px solid ${PRIMARY}`, position: 'sticky', top: 0, zIndex: 50 }}>
        <span style={{ fontWeight: 900, fontSize: '1.2rem', letterSpacing: '-0.02em', color: PRIMARY }}>{{BRAND_NAME}}</span>
        <div style={{ display: 'flex', gap: '2rem' }}>
          <a href="#products" className="bolt-nav-link" style={{ color: '#ccc', fontSize: '0.75rem', letterSpacing: '0.15em', textTransform: 'uppercase', textDecoration: 'none', fontWeight: 600 }}>Shop</a>
          <a href="#contact" className="bolt-nav-link" style={{ color: '#ccc', fontSize: '0.75rem', letterSpacing: '0.15em', textTransform: 'uppercase', textDecoration: 'none', fontWeight: 600 }}>Contact</a>
        </div>
      </nav>

      {/* Hero */}
      <section style={{ minHeight: '85dvh', display: 'flex', flexDirection: 'column', justifyContent: 'center', padding: '4rem 2.5rem', position: 'relative', overflow: 'hidden', background: `linear-gradient(135deg, #111 0%, #1a1a1a 100%)` }}>
        <div style={{ position: 'absolute', top: 0, right: 0, width: '40%', height: '100%', background: `linear-gradient(to left, ${PRIMARY}22, transparent)`, pointerEvents: 'none' }} />
        <p style={{
          color: ACCENT, fontSize: '0.7rem', letterSpacing: '0.4em', textTransform: 'uppercase', marginBottom: '1rem', fontWeight: 800,
          opacity: heroLoaded ? 1 : 0, transform: heroLoaded ? 'none' : 'translateY(24px)',
          transition: 'opacity 0.6s cubic-bezier(.22,1,.36,1) 0.1s, transform 0.6s cubic-bezier(.22,1,.36,1) 0.1s',
        }}>&#9889; {{HERO_LABEL}}</p>
        <h1 style={{
          fontSize: 'clamp(3rem,10vw,8rem)', fontWeight: 900, lineHeight: 0.9, letterSpacing: '-0.04em', margin: '0 0 1.5rem', textTransform: 'uppercase',
          opacity: heroLoaded ? 1 : 0, transform: heroLoaded ? 'none' : 'translateY(24px)',
          transition: 'opacity 0.6s cubic-bezier(.22,1,.36,1) 0.2s, transform 0.6s cubic-bezier(.22,1,.36,1) 0.2s',
        }}>
          {{HERO_HEADLINE}}
        </h1>
        <p style={{ color: 'rgba(255,255,255,0.55)', fontSize: '1.1rem', maxWidth: '38ch', lineHeight: 1.5, marginBottom: '2.5rem' }}>{{SLOGAN}}</p>
        <div style={{
          display: 'flex', gap: '1rem', flexWrap: 'wrap', alignItems: 'center',
          opacity: heroLoaded ? 1 : 0, transform: heroLoaded ? 'none' : 'translateY(24px)',
          transition: 'opacity 0.6s cubic-bezier(.22,1,.36,1) 0.35s, transform 0.6s cubic-bezier(.22,1,.36,1) 0.35s',
        }}>
          <a href="#products" className="bolt-btn" style={{ display: 'inline-flex', alignItems: 'center', gap: '0.5rem', background: PRIMARY, color: '#fff', fontWeight: 800, padding: '1rem 2.5rem', fontSize: '0.8rem', letterSpacing: '0.15em', textTransform: 'uppercase', textDecoration: 'none', boxShadow: `0 0 0 transparent` }}>
            Shop Nu &#8594;
          </a>
          <div style={{ display: 'flex', alignItems: 'center', gap: '0.5rem', color: 'rgba(255,255,255,0.4)', fontSize: '0.75rem' }}>
            <span style={{ color: ACCENT }}>&#9733;&#9733;&#9733;&#9733;&#9733;</span> 10.000+ tevreden klanten
          </div>
        </div>
        {/* Urgency */}
        <div style={{ marginTop: '2rem', display: 'inline-flex', alignItems: 'center', gap: '0.75rem', background: `${ACCENT}22`, border: `1px solid ${ACCENT}44`, padding: '0.6rem 1.25rem', borderRadius: '4px' }}>
          <span style={{ color: ACCENT, fontSize: '0.75rem', fontWeight: 800, textTransform: 'uppercase', letterSpacing: '0.1em' }}>&#9889; Beperkte voorraad</span>
          <span style={{ color: 'rgba(255,255,255,0.5)', fontSize: '0.75rem' }}>Besteld v&#243;&#243;r 22:00 = morgen in huis</span>
        </div>
      </section>

      {/* USP bar */}
      <div style={{ background: PRIMARY, padding: '1rem 2.5rem', display: 'flex', gap: '3rem', overflowX: 'auto', justifyContent: 'center', flexWrap: 'wrap' }}>
        {['{{USP_1_TITLE}}', '{{USP_2_TITLE}}', '{{USP_3_TITLE}}'].map((u,i) => (
          <span key={i} style={{ color: '#fff', fontSize: '0.7rem', letterSpacing: '0.2em', textTransform: 'uppercase', fontWeight: 700, whiteSpace: 'nowrap' }}>&#10003; {u}</span>
        ))}
      </div>

      {/* Products */}
      <section id="products" style={{ padding: '5rem 2.5rem' }}>
        <h2 style={{ fontSize: '2rem', fontWeight: 900, textTransform: 'uppercase', letterSpacing: '-0.02em', marginBottom: '2.5rem', borderLeft: `4px solid ${PRIMARY}`, paddingLeft: '1rem' }}>Gear Up</h2>
        <div ref={productsReveal.ref} style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill,minmax(260px,1fr))', gap: '1.5rem' }}>
          {PRODUCTS.map((p,i) => (
            <div key={i}
              onMouseEnter={() => setHoveredProduct(i)}
              onMouseLeave={() => setHoveredProduct(null)}
              style={{
                background: '#111', border: `1px solid ${hoveredProduct === i ? PRIMARY : '#222'}`, overflow: 'hidden', cursor: 'pointer',
                ...reveal(productsReveal.visible, i),
                transform: productsReveal.visible ? (hoveredProduct === i ? 'translateY(-4px)' : 'none') : 'translateY(20px)',
                boxShadow: hoveredProduct === i ? `0 12px 40px ${PRIMARY}33` : 'none',
                transition: `opacity 0.6s cubic-bezier(.22,1,.36,1) ${i * 110}ms, transform 0.4s cubic-bezier(.22,1,.36,1), box-shadow 0.4s cubic-bezier(.22,1,.36,1), border-color 0.2s`,
              }}>
              <div style={{ aspectRatio: '1/1', overflow: 'hidden', background: '#1a1a1a', position: 'relative' }}>
                {p.image && <img src={p.image} alt={p.title} style={{ width: '100%', height: '100%', objectFit: 'cover', transition: 'transform 0.4s cubic-bezier(.22,1,.36,1)', transform: hoveredProduct === i ? 'scale(1.06)' : 'scale(1)' }} />}
                {p.badge && <span style={{ position: 'absolute', top: '0.75rem', left: '0.75rem', background: ACCENT, color: '#000', fontSize: '0.65rem', fontWeight: 800, padding: '0.2rem 0.5rem', textTransform: 'uppercase', letterSpacing: '0.08em' }}>{p.badge}</span>}
              </div>
              <div style={{ padding: '1.25rem' }}>
                <p style={{ margin: '0 0 0.5rem', fontWeight: 700, fontSize: '0.9rem', textTransform: 'uppercase', letterSpacing: '0.05em' }}>{p.title}</p>
                <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
                  <div>
                    <span style={{ fontWeight: 800, fontSize: '1.1rem', color: PRIMARY }}>&#8364;{p.price.toFixed(2)}</span>
                    {p.compareAtPrice && <span style={{ color: '#555', fontSize: '0.85rem', textDecoration: 'line-through', marginLeft: '0.5rem' }}>&#8364;{p.compareAtPrice.toFixed(2)}</span>}
                  </div>
                  <button className="bolt-buy-btn" style={{ background: PRIMARY, color: '#fff', border: 'none', padding: '0.5rem 1rem', fontSize: '0.7rem', fontWeight: 800, textTransform: 'uppercase', letterSpacing: '0.1em', cursor: 'pointer' }}>
                    Kopen
                  </button>
                </div>
              </div>
            </div>
          ))}
        </div>
      </section>

      {/* Reviews */}
      <section style={{ padding: '4rem 2.5rem', background: '#111', borderTop: `2px solid ${PRIMARY}` }}>
        <h2 style={{ fontSize: '1.5rem', fontWeight: 900, textTransform: 'uppercase', marginBottom: '2rem' }}>Community</h2>
        <div ref={reviewsReveal.ref} style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit,minmax(240px,1fr))', gap: '1.5rem' }}>
          {[
            { n: 'Marc R.',   s: 5, t: 'Beste aankoop van het jaar. Kwaliteit is top!' },
            { n: 'Julia K.',  s: 5, t: 'Snel geleverd, verpakking netjes, product geweldig.' },
            { n: 'Daan V.',   s: 5, t: 'Gebruik het dagelijks &#8212; echt een verschil.' },
          ].map((r,i) => (
            <div key={i} style={{ background: '#1a1a1a', padding: '1.5rem', borderLeft: `3px solid ${PRIMARY}`, ...reveal(reviewsReveal.visible, i) }}>
              <div style={{ color: ACCENT, marginBottom: '0.75rem', fontSize: '0.85rem' }}>{'★'.repeat(r.s)}</div>
              <p style={{ color: 'rgba(255,255,255,0.65)', fontSize: '0.875rem', lineHeight: 1.6, marginBottom: '0.75rem' }} dangerouslySetInnerHTML={{ __html: `"${r.t}"` }} />
              <span style={{ fontWeight: 700, fontSize: '0.8rem', color: PRIMARY }}>{r.n}</span>
            </div>
          ))}
        </div>
      </section>

      {/* Footer */}
      <footer style={{ background: '#0a0a0a', borderTop: `2px solid ${PRIMARY}`, padding: '2rem 2.5rem', display: 'flex', justifyContent: 'space-between', alignItems: 'center', flexWrap: 'wrap', gap: '1rem' }}>
        <span style={{ fontWeight: 900, fontSize: '1rem', color: PRIMARY, textTransform: 'uppercase', letterSpacing: '-0.01em' }}>{{BRAND_NAME}}</span>
        <span style={{ color: '#444', fontSize: '0.75rem' }}>&#169; {year} {{BRAND_NAME}}</span>
      </footer>
    </div>
  );
}
