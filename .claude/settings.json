{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "shell": "powershell",
            "command": "Set-Location 'd:\\Dropshippingv0.1tool'; git add -A; $staged = git diff --cached --name-only; if ($staged) { $ts = Get-Date -Format 'yyyyMMdd-HHmmss'; git commit -m \"auto: $ts\"; git push origin main } 2>$null",
            "timeout": 30,
            "statusMessage": "Auto-pushing to git..."
          }
        ]
      }
    ]
  }
}
