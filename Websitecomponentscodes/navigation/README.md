# Navigation Bar

Responsive top navigation with logo, links, and cart icon.
Mobile hamburger menu with slide-in drawer.

## Props
- `brandName`: string — Store/brand name
- `logoUrl?`: string — Optional logo image
- `links`: NavLink[] — Navigation links
- `cartCount?`: number — Items in cart badge

## NavLink Shape
- `label`: string
- `href`: string
